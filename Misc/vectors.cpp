#include<bits/stdc++.h>
using namespace std;

int main(){
    vector<int> *v = new vector<int>(); // dynamically allocated, we have to delete it ourself
    vector<int> v; // statically allocated, deletes itself
    return 0;


    // we will use square brackets for update not for insert
}